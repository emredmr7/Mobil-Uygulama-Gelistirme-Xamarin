<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinFirebase.Views.Product.ChangePassword"
             BackgroundColor="#827098">
    <ContentPage.Content>
        <StackLayout Padding="16" VerticalOptions="Center">
            <Label Text="Şifreyi Değiştir"
                   TextColor="#F0EDEA"
                VerticalOptions="Center" 
                HorizontalOptions="Center"
                FontSize="Title"
                   FontAttributes="Bold"/>

            <Entry Placeholder="Yeni Şifre" x:Name="TxtPassword" IsPassword="True"  PlaceholderColor="#F0EDEA"  Margin="30,50,30,10" TextColor="#F0EDEA"></Entry>
            <Entry Placeholder="Yeni Şifre Tekrar" x:Name="TxtConfirmPassword" IsPassword="True" PlaceholderColor="#F0EDEA"  Margin="30,10,30,80" TextColor="#F0EDEA"></Entry>
            <Button Text="Şifre Değiştir" 
                    TextTransform="None"
                    x:Name="ChangePasswordButton"
                    Clicked="ChangePasswordButton_Clicked"
                    Padding="15"
                    Margin="30,30,30,50"
                    BackgroundColor="#F0EDEA"
                    TextColor="#827098"
                    CornerRadius="30"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>